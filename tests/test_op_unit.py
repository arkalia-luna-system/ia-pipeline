"""
Tests unitaires générés pour op
"""

import pytest
from pathlib import Path
import sys

# Ajouter le chemin du projet au PYTHONPATH
project_root = Path(__file__).parent.parent
sys.path.insert(0, str(project_root))

try:
    import op
except ImportError:
    pytest.skip(f"Module op non importable")


def test__visit_and_replace_children():
    """Test de la fonction _visit_and_replace_children"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_visit_and_replace_children')
    assert callable(getattr(op, '_visit_and_replace_children'))

def test__codegen_impl():
    """Test de la fonction _codegen_impl"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_codegen_impl')
    assert callable(getattr(op, '_codegen_impl'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__validate():
    """Test de la fonction _validate"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_validate')
    assert callable(getattr(op, '_validate'))

def test__visit_and_replace_children():
    """Test de la fonction _visit_and_replace_children"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_visit_and_replace_children')
    assert callable(getattr(op, '_visit_and_replace_children'))

def test__codegen_impl():
    """Test de la fonction _codegen_impl"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_codegen_impl')
    assert callable(getattr(op, '_codegen_impl'))

def test__get_tokens():
    """Test de la fonction _get_tokens"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_tokens')
    assert callable(getattr(op, '_get_tokens'))

def test__visit_and_replace_children():
    """Test de la fonction _visit_and_replace_children"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_visit_and_replace_children')
    assert callable(getattr(op, '_visit_and_replace_children'))

def test__codegen_impl():
    """Test de la fonction _codegen_impl"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_codegen_impl')
    assert callable(getattr(op, '_codegen_impl'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__codegen_impl():
    """Test de la fonction _codegen_impl"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_codegen_impl')
    assert callable(getattr(op, '_codegen_impl'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__validate():
    """Test de la fonction _validate"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_validate')
    assert callable(getattr(op, '_validate'))

def test__visit_and_replace_children():
    """Test de la fonction _visit_and_replace_children"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_visit_and_replace_children')
    assert callable(getattr(op, '_visit_and_replace_children'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_tokens():
    """Test de la fonction _get_tokens"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_tokens')
    assert callable(getattr(op, '_get_tokens'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_tokens():
    """Test de la fonction _get_tokens"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_tokens')
    assert callable(getattr(op, '_get_tokens'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

def test__get_token():
    """Test de la fonction _get_token"""
    # TODO: Implémenter les tests spécifiques
    assert hasattr(op, '_get_token')
    assert callable(getattr(op, '_get_token'))

class Test_BaseOneTokenOp:
    """Tests pour la classe _BaseOneTokenOp"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, '_BaseOneTokenOp')
        assert isinstance(getattr(op, '_BaseOneTokenOp'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, '_BaseOneTokenOp')
        for method_name in ['_visit_and_replace_children', '_codegen_impl', '_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class Test_BaseTwoTokenOp:
    """Tests pour la classe _BaseTwoTokenOp"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, '_BaseTwoTokenOp')
        assert isinstance(getattr(op, '_BaseTwoTokenOp'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, '_BaseTwoTokenOp')
        for method_name in ['_validate', '_visit_and_replace_children', '_codegen_impl', '_get_tokens']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestBaseUnaryOp:
    """Tests pour la classe BaseUnaryOp"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'BaseUnaryOp')
        assert isinstance(getattr(op, 'BaseUnaryOp'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'BaseUnaryOp')
        for method_name in ['_visit_and_replace_children', '_codegen_impl', '_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestBaseBooleanOp:
    """Tests pour la classe BaseBooleanOp"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'BaseBooleanOp')
        assert isinstance(getattr(op, 'BaseBooleanOp'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'BaseBooleanOp')
        for method_name in []:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestBaseBinaryOp:
    """Tests pour la classe BaseBinaryOp"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'BaseBinaryOp')
        assert isinstance(getattr(op, 'BaseBinaryOp'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'BaseBinaryOp')
        for method_name in []:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestBaseCompOp:
    """Tests pour la classe BaseCompOp"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'BaseCompOp')
        assert isinstance(getattr(op, 'BaseCompOp'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'BaseCompOp')
        for method_name in []:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestBaseAugOp:
    """Tests pour la classe BaseAugOp"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'BaseAugOp')
        assert isinstance(getattr(op, 'BaseAugOp'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'BaseAugOp')
        for method_name in []:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestSemicolon:
    """Tests pour la classe Semicolon"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'Semicolon')
        assert isinstance(getattr(op, 'Semicolon'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'Semicolon')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestColon:
    """Tests pour la classe Colon"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'Colon')
        assert isinstance(getattr(op, 'Colon'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'Colon')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestComma:
    """Tests pour la classe Comma"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'Comma')
        assert isinstance(getattr(op, 'Comma'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'Comma')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestDot:
    """Tests pour la classe Dot"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'Dot')
        assert isinstance(getattr(op, 'Dot'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'Dot')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestImportStar:
    """Tests pour la classe ImportStar"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'ImportStar')
        assert isinstance(getattr(op, 'ImportStar'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'ImportStar')
        for method_name in ['_codegen_impl']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestAssignEqual:
    """Tests pour la classe AssignEqual"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'AssignEqual')
        assert isinstance(getattr(op, 'AssignEqual'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'AssignEqual')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestPlus:
    """Tests pour la classe Plus"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'Plus')
        assert isinstance(getattr(op, 'Plus'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'Plus')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestMinus:
    """Tests pour la classe Minus"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'Minus')
        assert isinstance(getattr(op, 'Minus'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'Minus')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestBitInvert:
    """Tests pour la classe BitInvert"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'BitInvert')
        assert isinstance(getattr(op, 'BitInvert'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'BitInvert')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestNot:
    """Tests pour la classe Not"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'Not')
        assert isinstance(getattr(op, 'Not'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'Not')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestAnd:
    """Tests pour la classe And"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'And')
        assert isinstance(getattr(op, 'And'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'And')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestOr:
    """Tests pour la classe Or"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'Or')
        assert isinstance(getattr(op, 'Or'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'Or')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestAdd:
    """Tests pour la classe Add"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'Add')
        assert isinstance(getattr(op, 'Add'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'Add')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestSubtract:
    """Tests pour la classe Subtract"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'Subtract')
        assert isinstance(getattr(op, 'Subtract'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'Subtract')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestMultiply:
    """Tests pour la classe Multiply"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'Multiply')
        assert isinstance(getattr(op, 'Multiply'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'Multiply')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestDivide:
    """Tests pour la classe Divide"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'Divide')
        assert isinstance(getattr(op, 'Divide'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'Divide')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestFloorDivide:
    """Tests pour la classe FloorDivide"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'FloorDivide')
        assert isinstance(getattr(op, 'FloorDivide'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'FloorDivide')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestModulo:
    """Tests pour la classe Modulo"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'Modulo')
        assert isinstance(getattr(op, 'Modulo'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'Modulo')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestPower:
    """Tests pour la classe Power"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'Power')
        assert isinstance(getattr(op, 'Power'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'Power')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestLeftShift:
    """Tests pour la classe LeftShift"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'LeftShift')
        assert isinstance(getattr(op, 'LeftShift'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'LeftShift')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestRightShift:
    """Tests pour la classe RightShift"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'RightShift')
        assert isinstance(getattr(op, 'RightShift'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'RightShift')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestBitOr:
    """Tests pour la classe BitOr"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'BitOr')
        assert isinstance(getattr(op, 'BitOr'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'BitOr')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestBitAnd:
    """Tests pour la classe BitAnd"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'BitAnd')
        assert isinstance(getattr(op, 'BitAnd'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'BitAnd')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestBitXor:
    """Tests pour la classe BitXor"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'BitXor')
        assert isinstance(getattr(op, 'BitXor'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'BitXor')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestMatrixMultiply:
    """Tests pour la classe MatrixMultiply"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'MatrixMultiply')
        assert isinstance(getattr(op, 'MatrixMultiply'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'MatrixMultiply')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestLessThan:
    """Tests pour la classe LessThan"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'LessThan')
        assert isinstance(getattr(op, 'LessThan'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'LessThan')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestGreaterThan:
    """Tests pour la classe GreaterThan"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'GreaterThan')
        assert isinstance(getattr(op, 'GreaterThan'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'GreaterThan')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestEqual:
    """Tests pour la classe Equal"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'Equal')
        assert isinstance(getattr(op, 'Equal'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'Equal')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestLessThanEqual:
    """Tests pour la classe LessThanEqual"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'LessThanEqual')
        assert isinstance(getattr(op, 'LessThanEqual'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'LessThanEqual')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestGreaterThanEqual:
    """Tests pour la classe GreaterThanEqual"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'GreaterThanEqual')
        assert isinstance(getattr(op, 'GreaterThanEqual'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'GreaterThanEqual')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestNotEqual:
    """Tests pour la classe NotEqual"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'NotEqual')
        assert isinstance(getattr(op, 'NotEqual'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'NotEqual')
        for method_name in ['_validate', '_visit_and_replace_children', '_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestIn:
    """Tests pour la classe In"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'In')
        assert isinstance(getattr(op, 'In'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'In')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestNotIn:
    """Tests pour la classe NotIn"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'NotIn')
        assert isinstance(getattr(op, 'NotIn'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'NotIn')
        for method_name in ['_get_tokens']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestIs:
    """Tests pour la classe Is"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'Is')
        assert isinstance(getattr(op, 'Is'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'Is')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestIsNot:
    """Tests pour la classe IsNot"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'IsNot')
        assert isinstance(getattr(op, 'IsNot'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'IsNot')
        for method_name in ['_get_tokens']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestAddAssign:
    """Tests pour la classe AddAssign"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'AddAssign')
        assert isinstance(getattr(op, 'AddAssign'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'AddAssign')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestSubtractAssign:
    """Tests pour la classe SubtractAssign"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'SubtractAssign')
        assert isinstance(getattr(op, 'SubtractAssign'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'SubtractAssign')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestMultiplyAssign:
    """Tests pour la classe MultiplyAssign"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'MultiplyAssign')
        assert isinstance(getattr(op, 'MultiplyAssign'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'MultiplyAssign')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestMatrixMultiplyAssign:
    """Tests pour la classe MatrixMultiplyAssign"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'MatrixMultiplyAssign')
        assert isinstance(getattr(op, 'MatrixMultiplyAssign'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'MatrixMultiplyAssign')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestDivideAssign:
    """Tests pour la classe DivideAssign"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'DivideAssign')
        assert isinstance(getattr(op, 'DivideAssign'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'DivideAssign')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestModuloAssign:
    """Tests pour la classe ModuloAssign"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'ModuloAssign')
        assert isinstance(getattr(op, 'ModuloAssign'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'ModuloAssign')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestBitAndAssign:
    """Tests pour la classe BitAndAssign"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'BitAndAssign')
        assert isinstance(getattr(op, 'BitAndAssign'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'BitAndAssign')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestBitOrAssign:
    """Tests pour la classe BitOrAssign"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'BitOrAssign')
        assert isinstance(getattr(op, 'BitOrAssign'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'BitOrAssign')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestBitXorAssign:
    """Tests pour la classe BitXorAssign"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'BitXorAssign')
        assert isinstance(getattr(op, 'BitXorAssign'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'BitXorAssign')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestLeftShiftAssign:
    """Tests pour la classe LeftShiftAssign"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'LeftShiftAssign')
        assert isinstance(getattr(op, 'LeftShiftAssign'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'LeftShiftAssign')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestRightShiftAssign:
    """Tests pour la classe RightShiftAssign"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'RightShiftAssign')
        assert isinstance(getattr(op, 'RightShiftAssign'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'RightShiftAssign')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestPowerAssign:
    """Tests pour la classe PowerAssign"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'PowerAssign')
        assert isinstance(getattr(op, 'PowerAssign'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'PowerAssign')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

class TestFloorDivideAssign:
    """Tests pour la classe FloorDivideAssign"""

    def test_class_exists(self):
        """Vérifie que la classe existe"""
        assert hasattr(op, 'FloorDivideAssign')
        assert isinstance(getattr(op, 'FloorDivideAssign'), type)

    def test_class_methods(self):
        """Vérifie les méthodes de la classe"""
        cls = getattr(op, 'FloorDivideAssign')
        for method_name in ['_get_token']:
            assert hasattr(cls, method_name)
            assert callable(getattr(cls, method_name))

if __name__ == "__main__":
    pytest.main([__file__])
