api_spec:
  api:
    description: 'API principale (mode: fast/safe)'
    errors:
    - code: 401
      desc: Unauthorized
    - code: 400
      desc: Bad Request
    examples:
      request:
        data: test
        mode: fast
        user_id: 1
      response:
        details:
          info: "d\xE9tail"
        result: ok
    params:
      data: string
      mode:
        enum:
        - fast
        - safe
        type: string
      user_id: integer
    response:
      details: object
      result: string
  memory:
    description: "Stockage cl\xE9/valeur"
    errors:
    - code: 404
      desc: Not Found
    examples:
      request:
        key: foo
        value: bar
      response:
        status: ok
    params:
      key: string
      value: string
    response:
      status: string
  tts:
    description: "Synth\xE8se vocale (lang: fr/en)"
    errors:
    - code: 422
      desc: Unprocessable Entity
    examples:
      request:
        lang: fr
        text: Bonjour
      response:
        audio: base64...
    params:
      lang:
        enum:
        - fr
        - en
        type: string
      text: string
    response:
      audio: string
auth:
  example: sk-123456
  in: header
  name: X-API-KEY
  oauth2:
    flows:
      password:
        scopes:
          read: Lecture
          write: "\xC9criture"
        tokenUrl: /auth/token
    type: oauth2
  type: apiKey
booster_ia: true
dependencies:
- flask
- tts
- memorylib
description: Test YAML final
docker: false
modules:
- api
- tts
- memory
project_name: ia_project
prompts:
- dev_debug.yaml
- ux_fun_boost.md
structure:
- src/
- tests/
- api/
- prompts/
- README.md
- requirements.txt
