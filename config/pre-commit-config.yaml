# Configuration pre-commit pour Athalia
# Automatise les vérifications avant chaque commit

repos:
  # Hooks de base
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
        args: ['--maxkb=10240']
      - id: check-merge-conflict
      - id: check-json
      - id: check-toml
      - id: debug-statements
      - id: requirements-txt-fixer

  # Python - Formatage (DÉSACTIVÉ - géré manuellement)
  # - repo: https://github.com/psf/black
  #   rev: 23.12.1
  #   hooks:
  #     - id: black
  #       args: [--line-length=88]

  # Python - Organisation des imports (DÉSACTIVÉ - géré manuellement)
  # - repo: https://github.com/pycqa/isort
  #   rev: 5.13.2
  #   hooks:
  #     - id: isort
  #       args: [--profile=black, --line-length=88]

  # Python - Linting avec Ruff (DÉSACTIVÉ - géré manuellement)
  # - repo: https://github.com/astral-sh/ruff-pre-commit
  #   rev: v0.1.6
  #   hooks:
  #     - id: ruff
  #       args: [--fix, --exit-non-zero-on-fix]
  #     - id: ruff-format

  # Sécurité (désactivé temporairement pour permettre les commits)
  # - repo: https://github.com/PyCQA/bandit
  #   rev: 1.7.5
  #   hooks:
  #     - id: bandit
  #       args: [-r, athalia_core]

# Configuration globale
default_language_version:
  python: python3
