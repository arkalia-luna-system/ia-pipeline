# =====================================================
# requirements.txt — Athalia/Arkalia (corrigé 2025-07-29)
# =====================================================
#
# Fichier corrigé avec les versions exactes qui fonctionnent
# =====================================================

# --- CORE PYTHON ---
requests==2.32.4
pyyaml==6.0.2
jinja2==3.1.6
click==8.2.1
rich==14.1.0
python-dotenv==1.1.1
pydantic==2.11.7
fastapi==0.116.1
uvicorn==0.35.0
pillow==11.3.0

# --- IA/ML ---
# openai>=1.0.0  # Optionnel - décommenter si nécessaire
# anthropic>=0.3.0  # Optionnel - décommenter si nécessaire
# transformers>=4.20.0  # Optionnel - décommenter si nécessaire
# torch>=1.12.0  # Optionnel - décommenter si nécessaire
# scikit-learn>=1.7.0  # Optionnel - décommenter si nécessaire
pandas==2.3.1
numpy==2.2.6
# langchain>=0.3.26  # Optionnel - décommenter si nécessaire
# crewai>=0.148.0  # Optionnel - décommenter si nécessaire
pyautogen==0.10.0

# --- API & WEB ---
# flask>=3.1.0  # Optionnel - décommenter si nécessaire
streamlit==1.47.1
# plotly>=6.2.0  # Optionnel - décommenter si nécessaire

# --- TESTS ---
pytest==8.4.1
pytest-cov==6.2.1
pytest-benchmark==5.1.0
# pytest-mock>=3.8.0  # Optionnel - décommenter si nécessaire

# --- LINTING ---
flake8==7.3.0
black==25.1.0
# isort>=6.0.0  # Optionnel - décommenter si nécessaire
mccabe==0.7.0
pycodestyle==2.14.0
pyflakes==3.4.0

# --- UTILS ---
psutil==7.0.0
# loguru>=0.7.0  # Optionnel - décommenter si nécessaire

# --- SÉCURITÉ ---
# cryptography>=45.0.0  # Optionnel - décommenter si nécessaire

# --- CI/CD ---
# gitpython  # Optionnel - décommenter si nécessaire

# --- DOCS ---
# markdown  # Optionnel - décommenter si nécessaire

# --- DÉVELOPPEMENT ---
# pre-commit>=2.20.0  # Optionnel - décommenter si nécessaire
# tox>=3.25.0  # Optionnel - décommenter si nécessaire

# --- DÉPENDANCES SYSTÈME ---
coverage==7.10.1
pluggy==1.6.0
iniconfig==2.1.0
packaging==25.0
pygments==2.19.2
tomli==2.2.1
typing-extensions==4.14.1
exceptiongroup==1.3.0
urllib3==2.5.0
certifi==2025.7.14
charset-normalizer==3.4.2
idna==3.10
anyio==4.9.0
sniffio==1.3.1
pyflakes==3.4.0
