#!/bin/bash
# Script de vérification de l'état de propreté du projet
# Affiche un rapport détaillé sur l'état actuel du projet

set -e

# Couleurs
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
PURPLE='\033[0;35m'
CYAN='\033[0;36m'
NC='\033[0m'

echo -e "${PURPLE}🔍 Rapport d'état de propreté du projet Athalia${NC}"
echo -e "${PURPLE}==============================================${NC}"
echo ""

# Fonction pour afficher les résultats
show_section() {
    echo -e "${CYAN}📋 $1${NC}"
}

show_success() {
    echo -e "${GREEN}✅ $1${NC}"
}

show_warning() {
    echo -e "${YELLOW}⚠️  $1${NC}"
}

show_error() {
    echo -e "${RED}❌ $1${NC}"
}

show_info() {
    echo -e "${BLUE}ℹ️  $1${NC}"
}

# 1. Statistiques générales du projet
show_section "1. Statistiques générales du projet"
TOTAL_FILES=$(find . -type f | wc -l)
TOTAL_DIRS=$(find . -type d | wc -l)
PROJECT_SIZE=$(du -sh . | cut -f1)

echo "   Total fichiers: $TOTAL_FILES"
echo "   Total dossiers: $TOTAL_DIRS"
echo "   Taille du projet: $PROJECT_SIZE"
echo ""

# 2. État des fichiers temporaires macOS
show_section "2. Fichiers temporaires macOS"
DS_STORE_COUNT=$(find . -path "./.venv" -prune -o -path "./.git" -prune -o -name ".DS_Store" -print | wc -l)
APPLE_DOUBLE_COUNT=$(find . -path "./.venv" -prune -o -path "./.git" -prune -o -name "._*" -print | wc -l)

if [ $DS_STORE_COUNT -eq 0 ] && [ $APPLE_DOUBLE_COUNT -eq 0 ]; then
    show_success "Aucun fichier temporaire macOS détecté"
else
    show_warning "$DS_STORE_COUNT fichiers .DS_Store et $APPLE_DOUBLE_COUNT fichiers AppleDouble trouvés"
fi
echo ""

# 3. État des fichiers Python compilés
show_section "3. Fichiers Python compilés"
PYC_COUNT=$(find . -path "./.venv" -prune -o -name "*.pyc" -print | wc -l)
PYCACHE_COUNT=$(find . -path "./.venv" -prune -o -type d -name "__pycache__" -print | wc -l)

if [ $PYC_COUNT -eq 0 ] && [ $PYCACHE_COUNT -eq 0 ]; then
    show_success "Aucun fichier Python compilé détecté"
else
    show_warning "$PYC_COUNT fichiers .pyc et $PYCACHE_COUNT dossiers __pycache__ trouvés"
fi
echo ""

# 4. État des fichiers temporaires système
show_section "4. Fichiers temporaires système"
TEMP_FILES=$(find . -path "./.venv" -prune -o -type f \( -name "*.tmp" -o -name "*.temp" -o -name "*.swp" -o -name "*.swo" -o -name "*~" -o -name ".#*" -o -name "*.orig" -o -name "*.rej" -o -name "Thumbs.db" \) -print | wc -l)

if [ $TEMP_FILES -eq 0 ]; then
    show_success "Aucun fichier temporaire système détecté"
else
    show_warning "$TEMP_FILES fichiers temporaires système trouvés"
fi
echo ""

# 5. État des fichiers vides
show_section "5. Fichiers vides"
EMPTY_FILES=$(find . -path "./.venv" -prune -o -type f -empty -print | wc -l)
EMPTY_DIRS=$(find . -path "./.venv" -prune -o -type d -empty -print | wc -l)

if [ $EMPTY_FILES -eq 0 ] && [ $EMPTY_DIRS -eq 0 ]; then
    show_success "Aucun fichier ou dossier vide détecté"
else
    show_warning "$EMPTY_FILES fichiers vides et $EMPTY_DIRS dossiers vides trouvés"
fi
echo ""

# 6. État des logs
show_section "6. Fichiers de logs"
LOG_FILES=$(find . -path "./.venv" -prune -o -name "*.log" -print | wc -l)
OLD_LOG_FILES=$(find . -path "./.venv" -prune -o -name "*.log" -mtime +30 -print | wc -l)

if [ $LOG_FILES -eq 0 ]; then
    show_success "Aucun fichier de log détecté"
else
    show_info "$LOG_FILES fichiers de logs trouvés"
    if [ $OLD_LOG_FILES -gt 0 ]; then
        show_warning "$OLD_LOG_FILES logs anciens (>30 jours) détectés"
    else
        show_success "Tous les logs sont récents"
    fi
fi
echo ""

# 7. État des fichiers de cache
show_section "7. Fichiers de cache"
CACHE_FILES=$(find . -path "./.venv" -prune -o -name "*.cache" -o -name "*.cache.*" -print | wc -l)
MYPY_CACHE=$(find . -path "./.venv" -prune -o -name ".mypy_cache" -print | wc -l)

if [ $CACHE_FILES -eq 0 ] && [ $MYPY_CACHE -eq 0 ]; then
    show_success "Aucun fichier de cache détecté"
else
    show_info "$CACHE_FILES fichiers de cache et $MYPY_CACHE dossiers .mypy_cache trouvés"
fi
echo ""

# 8. État des permissions
show_section "8. Permissions des fichiers"
NON_EXECUTABLE_SCRIPTS=$(find bin/ -type f ! -perm +111 2>/dev/null | wc -l)

if [ $NON_EXECUTABLE_SCRIPTS -eq 0 ]; then
    show_success "Tous les scripts dans bin/ sont exécutables"
else
    show_warning "$NON_EXECUTABLE_SCRIPTS scripts non exécutables dans bin/"
fi
echo ""

# 9. État du .gitignore
show_section "9. Configuration .gitignore"
if [ -f ".gitignore" ]; then
    show_success "Fichier .gitignore présent"
    IGNORE_PATTERNS=$(grep -c "^[^#]" .gitignore 2>/dev/null || echo "0")
    show_info "$IGNORE_PATTERNS patterns de fichiers ignorés"
else
    show_error "Fichier .gitignore manquant"
fi
echo ""

# 10. Score de propreté
show_section "10. Score de propreté du projet"
TOTAL_ISSUES=$((DS_STORE_COUNT + APPLE_DOUBLE_COUNT + PYC_COUNT + PYCACHE_COUNT + TEMP_FILES + EMPTY_FILES + EMPTY_DIRS + OLD_LOG_FILES))

if [ $TOTAL_ISSUES -eq 0 ]; then
    show_success "🎉 Score parfait : 100/100"
    echo "   Le projet est parfaitement propre !"
elif [ $TOTAL_ISSUES -lt 10 ]; then
    show_success "✅ Score excellent : 90-99/100"
    echo "   Le projet est très propre avec seulement $TOTAL_ISSUES éléments à nettoyer"
elif [ $TOTAL_ISSUES -lt 50 ]; then
    show_warning "⚠️  Score bon : 70-89/100"
    echo "   Le projet est propre avec $TOTAL_ISSUES éléments à nettoyer"
else
    show_error "❌ Score faible : <70/100"
    echo "   Le projet nécessite un nettoyage : $TOTAL_ISSUES éléments à traiter"
fi
echo ""

# 11. Recommandations
show_section "11. Recommandations"

if [ $TOTAL_ISSUES -eq 0 ]; then
    show_info "💡 Maintenez cette propreté :"
    echo "   - Exécutez ath-cleanup-preventive hebdomadairement"
    echo "   - Surveillez les nouveaux types de fichiers"
    echo "   - Mettez à jour .gitignore si nécessaire"
else
    show_info "🧹 Actions recommandées :"
    if [ $DS_STORE_COUNT -gt 0 ] || [ $APPLE_DOUBLE_COUNT -gt 0 ]; then
        echo "   - Exécutez : ./bin/ath-cleanup-preventive"
    fi
    if [ $PYC_COUNT -gt 0 ] || [ $PYCACHE_COUNT -gt 0 ]; then
        echo "   - Nettoyez les fichiers Python compilés"
    fi
    if [ $OLD_LOG_FILES -gt 0 ]; then
        echo "   - Supprimez les logs anciens"
    fi
    echo "   - Exécutez : ./bin/ath-cleanup-complete pour un nettoyage complet"
fi
echo ""

# 12. Résumé final
show_section "12. Résumé final"
echo "   ${BLUE}📊 État général:${NC}"
echo "     - Fichiers temporaires macOS: $((DS_STORE_COUNT + APPLE_DOUBLE_COUNT))"
echo "     - Fichiers Python compilés: $((PYC_COUNT + PYCACHE_COUNT))"
echo "     - Fichiers temporaires système: $TEMP_FILES"
echo "     - Fichiers/dossiers vides: $((EMPTY_FILES + EMPTY_DIRS))"
echo "     - Logs anciens: $OLD_LOG_FILES"
echo "     - Fichiers de cache: $((CACHE_FILES + MYPY_CACHE))"
echo ""
echo "   ${GREEN}🎯 Total problèmes: $TOTAL_ISSUES${NC}"
echo ""

if [ $TOTAL_ISSUES -eq 0 ]; then
    show_success "🌟 Le projet Athalia est en excellent état !"
else
    show_warning "🔧 Quelques améliorations possibles pour optimiser le projet"
fi
